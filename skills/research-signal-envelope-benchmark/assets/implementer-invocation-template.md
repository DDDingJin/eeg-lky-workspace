Use `$research-signal-envelope-benchmark` as `implementer` together with `$research-audit-loop`.

1. Load the project workflow state, accepted issues, target commit, and active domain gate.
2. Modify only implementer-owned code, tests, configs, results, and implementation response.
3. Run bounded checks and the smallest affected domain gate.
4. Prepare exact user commands for long runs.
5. Record incidents, artifacts, commands, and remaining limits.
6. Commit, attempt push once, verify the remote commit, and report the formal publication state.
