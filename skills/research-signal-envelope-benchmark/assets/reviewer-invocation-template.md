Use `$research-signal-envelope-benchmark` as `reviewer` together with `$research-audit-loop`.

1. Load the project workflow state and the version-locked audit round.
2. Inspect the active scientific phase, code, resolved config, raw evidence, domain gate, and claim support.
3. Preserve reviewer/implementer file ownership.
4. Report structured findings with acceptance checks.
5. Do not implement the reviewed fix.
6. Verify only the merged fix commit after both audit and domain gates pass.
